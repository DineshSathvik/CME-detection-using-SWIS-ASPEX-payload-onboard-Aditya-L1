# CME Detection (Coronal Mass Ejection Detection)

This project detects CMEs from solar observation CDF files using anomaly detection (Isolation Forest).

## Installation
```bash
git clone https://github.com/yourusername/cme-detection.git
cd cme-detection
pip install -r requirements.txt
